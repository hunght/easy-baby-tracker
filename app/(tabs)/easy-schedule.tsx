import EasyScheduleScreen from '../(easy-schedule)/easy-schedule';

// Render the full E.A.S.Y. schedule experience directly in the tab
export default function EasyScheduleTab() {
  return <EasyScheduleScreen />;
}
